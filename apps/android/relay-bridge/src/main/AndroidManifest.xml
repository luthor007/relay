<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
      The vendor sample declares none of this: it is Activity-only, so capture
      dies the moment the app leaves the foreground. Everything below exists to
      keep a BLE link and a capture session alive for a working day.
    -->

    <!-- BLE. NEVER_FOR_LOCATION matters: without it, Android 12+ forces the
         app to hold location permission just to scan, which is a permission
         prompt we have no business showing for a pair of glasses. -->
    <uses-permission
        android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="s" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <!-- Legacy BLE, capped so they are not requested on modern releases. -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />

    <!-- All-day capture reads audio off the glasses over BLE and never opens
         AudioRecord, so the running service declares connectedDevice only. This
         permission and the FGS type below exist for the phone-side recorder —
         the wake-word spotter of ARCHITECTURE.md §5.2b — which is opt-in and
         adds the type at the moment it opens the mic. See
         RelayCaptureService.promoteToForeground for why that split matters at
         boot as well as in the status bar. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Foreground service. The typed permissions are mandatory from API 34,
         and a service must hold the permission matching every type it declares.
         Note deliberately absent: FOREGROUND_SERVICE_DATA_SYNC. The nightly
         WiFi sync runs inside the existing connectedDevice service rather than
         a dataSync one, because dataSync carries a per-day runtime cap on
         recent releases and a capture service that stops after N hours is the
         exact failure this module exists to prevent. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />

    <!-- The ongoing notification is the user-visible proof that capture is
         running. On API 33+ posting it needs runtime consent. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Bulk sync joins the glasses' own access point. -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />

    <!-- Requested only from a screen that explains why, never silently. -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <uses-feature android:name="android.hardware.bluetooth_le" android:required="true" />

    <application>

        <service
            android:name=".RelayCaptureService"
            android:exported="false"
            android:foregroundServiceType="connectedDevice|microphone"
            android:description="@string/relay_service_description" />

        <receiver
            android:name=".BootReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <!-- Several OEM skins send this instead of, or as well as,
                     BOOT_COMPLETED after their own "optimised" restart. -->
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
