# Generated by xcodegen from project.yml, which is the file worth reviewing.
# A pbxproj is a merge-conflict generator.
Relay.xcodeproj/

# `xcodebuild -derivedDataPath .build` — see README. The machine's default
# DerivedData path does not exist, so builds have to name one.
.build/
